実行方法は
- zipを解凍
- フォルダをcmdで開く
- `py -m venv venv` で環境変数作成
- `./venv/bin/activate` で環境変数アタッチ
- `pip install -r requirements.txt` で必要なライブラリをインストール
- `py main.py` で実行
pdfフォルダーが作成されて、その中にPDFがたくさんDLされます